Write the implementation plan.
1. review the existing local code base for existing patterns.
  - Noting that most of whats here in this local codebase is a derative of our existing FE/BE for the web app: you can use the web repo as reference for all FE work (../web) which already has a pattern for most of the react that we need. you can use helpers repo to see all endpoints related to datstores/filestores/kbs (../firebase-helpers) these are the skills*.pinkfish.ai endpoints. you can see platform repo for all /service endpoints that relate to workflows and agents and mcps (../platform). see pinkfish-connections repo for connections (../pinkfish-connections), these are the proxy*.pinkfish.ai endpoints. note that 
2. make plan for the most minimal change possible to achieve the brief.
3. write plan to auto-dev/plans dir in name format: YY-MM-DD-PIN#(from linear)-short name
4. plan should wind up with a check list of items to do, which should include test plan

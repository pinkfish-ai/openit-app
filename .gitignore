.vite

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Dev credentials (keychain bypass) — also catch .disabled / .bak renames
# so a "rename aside" doesn't accidentally commit creds.
.env.development
.env*.disabled
.env*.bak
.env*.bak.*

# Ephemeral runtime state from the autonomous-dev /loop driver.
# Machine-specific (session id, pid, start time); commits cause noise
# and merge conflicts.
.claude/scheduled_tasks.lock
